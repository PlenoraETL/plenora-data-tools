{
  "schema_version": 5,
  "inputs": ["citta"],
  "nodes": [
    {
      "id": "grandi",
      "op": "table.filter",
      "in": ["citta"],
      "config": { "column": "abitanti", "operator": ">", "value": 300000 }
    },
    {
      "id": "ordinate",
      "op": "table.sort",
      "in": ["grandi"],
      "config": { "columns": ["abitanti"], "ascending": false }
    }
  ],
  "output": "ordinate"
}
